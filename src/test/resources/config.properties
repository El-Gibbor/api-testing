# Default test environment configuration.
# Override without touching this file via:
#   -Dbase.uri=https://... (system property), or
#   BASE_URI=https://...   (environment variable)
base.uri=https://jsonplaceholder.typicode.com
